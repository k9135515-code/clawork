# Echo Skill

This folder contains a minimal valid WebAssembly module (`echo.wasm`) used to smoke-test the skill loader and hot-reload pipeline.

The current runtime validates module loading and ABI envelope wiring. Full stdin/stdout execution wiring is staged for the next implementation iteration.
